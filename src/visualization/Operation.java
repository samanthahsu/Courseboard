package visualization;

/** global shorthand for operations
 * used when updating observers
 * eg. faculty and boardManager
 * eg. courseNode and BoardManager
**/
public enum Operation {
    ADD, REMOVE;
}
